# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node / frontend build
node_modules/
frontend/dist/
*.tsbuildinfo

# Local environment — never commit a database url
.env
.env.local

# macOS
.DS_Store

# Database dumps — ~100 MB each, and a snapshot of data that is rebuilt, not versioned
*.dump
deploy/dump/

# Python build artefacts (the PyPI stub)
packaging/*/dist/
packaging/*/*.egg-info/
